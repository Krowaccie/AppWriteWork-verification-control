name: Trusted source artifact launcher
description: Inert template; Phase B1 must bind the reviewed immutable OCI image.
inputs:
  request:
    description: Canonical verification-a1-hosted-request.v1 JSON.
    required: true
runs:
  using: docker
  image: 'docker://ghcr.io/krowaccie/appwritework-verification-a1@sha256:c7c308d88c503963c39976b267df515d522cbfdd3a652163a2b957691a936b4b'
  args:
    - '${{ inputs.request }}'
